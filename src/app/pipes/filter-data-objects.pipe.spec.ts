import { FilterDataObjectsPipe } from './filter-data-objects.pipe';

describe('FilterDataObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
